#ifndef _LISTA_
#define  _LISTA_

#include <iostream>
#include <string>

using namespace std;

struct TNoticia {
    string titular;
    struct noticia* next = nullptr;
    struct noticia* prev = nullptr;


}*PtrTNoticia;

void insertarInicio(){

    

    }

/*
void insertar(Node pData);// para insertar elementos en listas

void delete(Node pData);// borrar toda la lista al terminar de usarla

Node *listar();// imprimir todos los elementos de la lista

Node *buscar(string palabraDeBusqueda);// buscar elementos especificos

Node *eliminar(string palabraDebusqueda);// eliminar elemento especifico
*/

#endif